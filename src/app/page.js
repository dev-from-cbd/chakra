// Import the 'Image' component from the 'next/image' module
import Image from "next/image";

// Import styles from a local CSS module named 'page.module.css'
import styles from "./page.module.css";

// Define the default function 'Home' for the home page
export default function Home() {
  // Return the main content with the text 'Main'
  return <main>Main</main>;
}
